1. **"Update the chart configuration to show a 'Warning' color state (like solid Orange) when the total expenses cross 75% of the total salary, instead of waiting until it reaches 90%."**
2. **"Enhance the empty state of the expenses list by adding a subtle CSS pulse animation to the inbox icon and changing the 'No expenses logged yet' text color to match the primary brand blue."**
3. **"Fix the logic bug where users can accidentally submit an expense with an empty name consisting only of whitespace characters. Add a trim validation check before saving."**
